---
description: 查看 SEED_DATA 完整统计：phases、rounds、items 及分布
---

Generate a complete statistical overview of the SEED_DATA in `index.html`. Include:

1. Total counts: phases, rounds, items
2. Per-phase breakdown: phase name, round count, item count
3. Per-phase item list: round title + item count
4. Any empty rounds (rounds with 0 items)
5. Dimension value distribution (which levels are used how many times)

Use PowerShell to parse and report. Be thorough — every phase and round should be accounted for.
