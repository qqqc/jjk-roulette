---
description: 在浏览器中打开 index.html 进行试玩
---

Open `index.html` in the default browser. If the file is at a relative path, resolve it to an absolute path first. Use `Start-Process` on Windows to open it.
